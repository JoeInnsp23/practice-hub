/**
 * Global color constants for contexts that cannot reference CSS variables.
 *
 * These values mirror the semantic design tokens defined in globals.css so
 * inline styles (canvas charts, email templates, etc.) stay aligned with the
 * UI palette.
 */

/** Muted foreground color (light theme) used for neutral accents and footers. */
export const MUTED_FOREGROUND_HEX_LIGHT = "#4e565f";

/** Muted foreground color (dark theme) used for neutral accents and footers. */
export const MUTED_FOREGROUND_HEX_DARK = "#8b9095";
